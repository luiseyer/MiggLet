{
  "name": "migglet",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "engines": {
    "npm": ">= 7.9.0",
    "node": ">= 18.11.0"
  },
  "workspaces": [
    "api",
    "app"
  ],
  "scripts": {
    "build": "",
    "dev:api": "clear && npm run dev --workspace=api",
    "test:api": "clear && npm run test --workspace=api"
  },
  "devDependencies": {
    "standard": "17.0.0"
  },
  "eslintConfig": {
    "extends": "./node_modules/standard/eslintrc.json",
    "env": {
      "jest": true
    }
  }
}
